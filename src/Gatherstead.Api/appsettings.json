{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "//": "Base config carries NO connection string. Production supplies ConnectionStrings:Default via the platform (managed identity — see appservice.bicep); local development supplies it via appsettings.Development.json or user-secrets. Leaving this empty means a missing prod override fails closed rather than silently falling back to a local, certificate-trusting string.",
    "Default": ""
  },
  "ExternalIdentity": {
    "Instance": "https://your-tenant.ciamlogin.com",
    "Domain": "your-tenant.onmicrosoft.com",
    "ClientId": "your-client-id",
    "SignUpSignInPolicyId": "",
    "ValidIssuer": "https://your-tenant.ciamlogin.com/your-tenant-id/v2.0/"
  },
  "RateLimiting": {
    "PermitLimit": 300,
    "WindowMinutes": 1,
    "QueueLimit": 0
  },
  "AuthCache": {
    "UserMappingMinutes": 60,
    "AppAdminMinutes": 5,
    "RevokedSeconds": 60,
    "RoleSeconds": 60
  },
  "Cors": {
    "AllowedOrigins": []
  }
}
