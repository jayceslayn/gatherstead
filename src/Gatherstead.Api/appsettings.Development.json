{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "//": "Local Docker SQL Server only. TrustServerCertificate=True is appropriate here because the local container uses a self-signed certificate; it must never appear in a non-local environment (production connects via managed identity — see appservice.bicep).",
    "Default": "Server=localhost;Database=GathersteadDev;User Id=sa;Password=YourStrong@Passw0rd;TrustServerCertificate=True;Column Encryption Setting=Enabled;Attestation Protocol=HGS;"
  },
  "ExternalIdentity": {
    "Instance": "https://your-tenant.ciamlogin.com",
    "Domain": "your-tenant.onmicrosoft.com",
    "ClientId": "your-dev-client-id",
    "SignUpSignInPolicyId": "",
    "ValidIssuer": "https://your-tenant.ciamlogin.com/your-tenant-id/v2.0/"
  },
  "Cors": {
    "AllowedOrigins": [
      "http://localhost:3000",
      "http://localhost:5173"
    ]
  }
}
